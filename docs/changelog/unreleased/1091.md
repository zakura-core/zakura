## Fixed

- Legacy fallback can transfer hung Full Accepted applies (not only Checkpoint)
  when yielding the lease, so `FallbackDraining` is not wedged until the drain
  deadline
  ([#1091](https://github.com/zakura-core/zakura/pull/1091)).
