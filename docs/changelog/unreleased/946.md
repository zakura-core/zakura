## Fixed

- `submitblock` checks the proof of work before it holds a mined-submission slot, so a caller
  sending invalid blocks can no longer make a solved block wait for a slot and receive
  `inconclusive` ([#946](https://github.com/zakura-core/zakura/pull/946)).
