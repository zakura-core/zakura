[package]
name = "zakura-state"
version = "7.0.0-rc0"
authors.workspace = true
description = "State contextual verification and storage code for the Zakura node. Internal crate, published to support cargo install zakura"
license.workspace = true
repository.workspace = true
edition.workspace = true
rust-version.workspace = true

readme = "../../README.md"
homepage.workspace = true
# crates.io is limited to 5 keywords and categories
keywords.workspace = true
# Must be one of <https://crates.io/category_slugs>
categories = ["asynchronous", "caching", "cryptography::cryptocurrencies"]

[features]

# Production features that activate extra dependencies, or extra features in dependencies

# Exposes narrow internal hooks used by benchmarks.
internal-bench = []

progress-bar = [
    "howudoin",
]

# Indexes spending transaction ids by spent outpoints and revealed nullifiers
indexer = []

# Emit fine-grained per-block commit-phase timing histograms (zakura.state.write.*),
# used to profile and benchmark the finalized-state commit path. Off by default so
# the timing has zero overhead in production builds; enable for perf investigations.
commit-metrics = []

# Test-only features
header-fuzz = []
proptest-impl = [
    "proptest",
    "proptest-derive",
    "zakura-chain/proptest-impl"
]

[dependencies]
bincode = { workspace = true }
chrono = { workspace = true, features = ["clock", "std"] }
futures = { workspace = true }
hex = { workspace = true }
hex-literal = { workspace = true }
humantime-serde = { workspace = true }
indexmap = { workspace = true }
itertools = { workspace = true }
lazy_static = { workspace = true }
metrics = { workspace = true }
regex = { workspace = true }
rlimit = { workspace = true }
rocksdb = { workspace = true, features = ["lz4"] }
semver = { workspace = true }
crossbeam-channel = { workspace = true }
serde = { workspace = true, features = ["serde_derive"] }
serde-big-array = { workspace = true }
tempfile = { workspace = true }
thiserror = { workspace = true }

rayon = { workspace = true }
sha2 = { workspace = true }
tokio = { workspace = true, features = ["rt-multi-thread", "sync", "tracing", "macros", "time"] }
tower = { workspace = true, features = ["buffer", "util"] }
tracing = { workspace = true }

sapling-crypto = { workspace = true }

zakura-node-services = { path = "../zakura-node-services", version = "3.2.1-rc0" }
zakura-chain = { path = "../zakura-chain", version = "6.0.0-rc0", features = ["async-error"] }
zakura-header-chain = { path = "../zakura-header-chain", version = "1.0.0-rc0" }

# prod feature progress-bar
howudoin = { workspace = true, optional = true }

# test feature proptest-impl
proptest = { workspace = true, optional = true }
proptest-derive = { workspace = true, optional = true }
derive-getters.workspace = true
derive-new.workspace = true

[dev-dependencies]
zakura-header-chain = { path = "../zakura-header-chain", version = "1.0.0-rc0", features = ["test-support"] }
color-eyre = { workspace = true }

once_cell = { workspace = true }
spandoc = { workspace = true }
toml = { workspace = true }

hex = { workspace = true, features = ["serde"] }
insta = { workspace = true, features = ["ron", "redactions"] }
serde_json = { workspace = true }

proptest = { workspace = true }
proptest-derive = { workspace = true }
rand = { workspace = true }

halo2 = { workspace = true }
orchard = { workspace = true }

tokio = { workspace = true, features = ["full", "tracing", "test-util"] }

zakura-chain = { path = "../zakura-chain", version = "6.0.0-rc0", features = ["proptest-impl"] }
zakura-test = { path = "../zakura-test/", version = "2.1.0" }

[lints]
workspace = true

# Removing the experimental `elasticsearch` feature is intentional. It never
# provided a supported downstream library API, so do not require a major
# version solely to remove its Cargo feature name.
[package.metadata.cargo-semver-checks.lints]
feature_missing = "warn"

[[bench]]
name = "transparent_spend_worst_case"
harness = false
required-features = ["internal-bench"]

[[bench]]
name = "finality_witness"
harness = false
required-features = ["internal-bench"]
