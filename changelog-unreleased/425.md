### Changed

- Orchard `cv` and `ephemeral_key` are now stored as raw 32-byte encodings and
  their point decompression checks run during semantic verification instead of
  deserialization, removing two Pallas square roots per action from the
  checkpoint-sync hot path. Wire formats, txids, sighashes, and semantic-path
  rejection behavior are unchanged.
