## Fixed

- `getblockchaininfo` omits `nsmValueBalanceZat` when tip pool values are
  unavailable, instead of reporting a synthetic zero that was indistinguishable
  from a known zero balance
  ([#1107](https://github.com/zakura-core/zakura/pull/1107)).
