## Fixed

- Header sync now repairs contiguous ranges of missing VCT roots in one
  request. Checkpoint commits now resume immediately after header insertion
  instead of polling every 500 ms. These changes improve checkpoint catch-up
  for retained states with large missing-root gaps
  ([#893](https://github.com/zakura-core/zakura/pull/893)).
