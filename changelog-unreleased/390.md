<!-- changelog: none -->

This PR only adds internal state database scaffolding for a durable
header-root authentication frontier (empty CF, helpers, and unregistered
cutover code). Provisional header-sync root persistence is unchanged, and
the format migration is not enabled yet.
