<!-- changelog: none -->

This PR only moves the workspace crates into the `crates/` directory and updates
path references (manifests, CI, scripts, docker, docs). It has no operator- or
crate-consumer-visible effect.
