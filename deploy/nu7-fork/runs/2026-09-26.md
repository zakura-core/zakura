# September 26, 2026 activation rehearsal

The public staging fork was reset onto PR #1097 rebased on main
`a3c599392af5dbffbe9acb784289c7c12ce04dda`. The node, miner, and faucet sender
were built together on Linux from
`738d175061e23d1ad65ec99b2d2a6b5d004bb10f` with `cargo build --release
--locked -p zakura -p zakura-fork-miner -p zakura-fork-txload`.
Later commits in this PR update deployment scripts and documentation, not those
Rust binaries. The node reports `1.4.0+119.g738d17506`.

A copy of the preserved public Testnet seed was caught up with that node to
height **4,398,752**. Its tip was one minute old when recorded, and the node
was stopped to flush finalized and non-finalized state before copying it.

- Seed hash:
  `000021f06eecebb18c50d5bda251f52be71a186092d3558278f6cd8eef84a50b`
- Network name: `Nu7StagingV2`; magic: `7a6b7538`.
- Activation: **4,398,756**, four blocks above the recorded seed.
- NU7 branch ID: `77190ad9`.
- Activation block:
  `035144c0ff852897608979e863e0210809ee08c3fe9bf35cfab4a9c09649e08d`
- The primary accepted activation at **19:27:54 UTC**; both local validators
  confirmed the same activation hash and reported NU7 active.
- NSM balance at activation: **55,768,414,957 zatoshi**.

This run uses main's consensus rules, including 100-block coinbase maturity.
The earlier fork's database, configurations, and binaries were retained for
rollback. Its faucet claims database is retained; the new run uses a separate
claims database and reports not ready until rewards mature.

The public [network manifest](https://api.nu7.valargroup.dev/v1/network)
provides the current participant config and matching snapshot. Treat the
manifest as authoritative for joining; the historical values in this report
are evidence of this particular rehearsal. Website PR #126 reads that source
instead of maintaining another activation height.

The snapshot contains only `state` and `non_finalized_state`, renamed for the
new network, and was taken before any fork blocks were mined. Participants
extract it into an empty `nu7-state` directory. SHA-256:
`4a347a643eee9a33f0b56a0f28e365c63ffbc0d5cc4ee46cd5d7cd1929117595`.
