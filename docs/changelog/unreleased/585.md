<!-- changelog: none -->

This PR only adds a benchmark target (`zakura-consensus`'s
`worst_case_tx_verification`, ported from Zebra so the two trees can be compared
on identical mainnet vectors) and registers it in the existing
`workflow_dispatch`-triggered benchmarks workflow. No library or binary code
changes, so there is no operator- or crate-consumer-visible effect.
