## Fixed

- `submitblock` rejects invalid proof of work before reserving a mined-submission slot.
  These submissions no longer exhaust slots and cause solved blocks to receive `inconclusive`.
  The RPC preserves proof-of-work waivers on regtest and configured custom networks
  ([#946](https://github.com/zakura-core/zakura/pull/946)).
