## Changed

- Replaced the unreleased standalone `export-historical-treestates` command and its archive and
  VCT modes with checkpoint export that extends the roots already in the binary using retained
  database rows ([#699](https://github.com/zakura-core/zakura/pull/699)).
