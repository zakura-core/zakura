<!-- changelog: none -->

This PR adds reusable bounded decoding APIs and preserves existing streaming
callers. Network message boundaries do not select the new APIs yet, so there
is no operator-visible behavior change.
