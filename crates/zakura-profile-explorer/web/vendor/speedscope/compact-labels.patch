diff --git a/src/lib/compact-frame.ts b/src/lib/compact-frame.ts
new file mode 100644
index 0000000..40b84b5
--- /dev/null
+++ b/src/lib/compact-frame.ts
@@ -0,0 +1,20 @@
+// Display only. Frame keys, search, exports and inspection keep the full symbol.
+export function compactFrame(name: string): string {
+  const symbol = name.replace(/ \(.*\)$/, '').replace(/::h[0-9a-f]{16}$/, '')
+  const qualified = symbol.startsWith('<')
+  const visibleDepth = qualified ? 1 : 0
+  let compact = '', depth = 0
+  for (const char of symbol) {
+    if (char === '<') {
+      if (depth === visibleDepth) compact += '<…>'
+      else if (qualified && depth === 0) compact += '<'
+      depth++
+    } else if (char === '>' && depth > 0) {
+      depth--
+      if (qualified && depth === 0) compact += '>'
+    } else if (depth <= visibleDepth) compact += char
+  }
+  if (qualified) return compact
+  const parts = compact.split('::')
+  return parts.length > 4 ? parts[0] + '::…::' + parts.slice(-3).join('::') : compact
+}
diff --git a/src/lib/profile.test.ts b/src/lib/profile.test.ts
index 0920d11..1b93d5a 100644
--- a/src/lib/profile.test.ts
+++ b/src/lib/profile.test.ts
@@ -333,3 +333,31 @@ test('getProfileWithRecursionFlattened', () => {
   const allFrameKeys = new Set([fa, fb].map(f => f.key))
   expect(allFrameKeys).toEqual(framesInProfile)
 })
+
+test('sample counts survive weighted aggregation and caller/callee views', () => {
+  const b = new StackListProfileBuilder()
+  const root = {key: 'root', name: 'root'}
+  const leaf = {key: 'leaf', name: 'leaf'}
+  b.appendSampleWithWeight([root, leaf], 2)
+  b.appendSampleWithWeight([root, leaf], 5)
+  b.appendSampleWithWeight([root], 3)
+  const p = b.build()
+  p.forEachCallGrouped((node) => {
+    if (node.frame.key === 'root') {
+      expect(node.getTotalWeight()).toBe(10)
+      expect(node.getTotalSamples()).toBe(3)
+      expect(node.getSelfSamples()).toBe(1)
+    }
+    if (node.frame.key === 'leaf') {
+      expect(node.getTotalWeight()).toBe(7)
+      expect(node.getTotalSamples()).toBe(2)
+      expect(node.getSelfSamples()).toBe(2)
+    }
+  }, () => {})
+  p.getProfileForCalleesOf(root).forEachFrame(frame => {
+    if (frame.key === 'root') expect(frame.getTotalSamples()).toBe(3)
+  })
+  p.getInvertedProfileForCallersOf(leaf).forEachFrame(frame => {
+    if (frame.key === 'leaf') expect(frame.getTotalSamples()).toBe(2)
+  })
+})
diff --git a/src/lib/profile.ts b/src/lib/profile.ts
index e34ac58..16a13f5 100644
--- a/src/lib/profile.ts
+++ b/src/lib/profile.ts
@@ -27,6 +27,20 @@ export type SymbolRemapper = (
 export class HasWeights {
   private selfWeight = 0
   private totalWeight = 0
+  private selfSamples: number | null = null
+  private totalSamples: number | null = null
+  getSelfSamples() {
+    return this.selfSamples ?? (this.totalSamples !== null ? 0 : null)
+  }
+  getTotalSamples() {
+    return this.totalSamples
+  }
+  addToSelfSamples(count: number | null) {
+    this.selfSamples = count === null ? null : (this.selfSamples || 0) + count
+  }
+  addToTotalSamples(count: number | null) {
+    this.totalSamples = count === null ? null : (this.totalSamples || 0) + count
+  }
   getSelfWeight() {
     return this.selfWeight
   }
@@ -43,6 +57,8 @@ export class HasWeights {
   overwriteWeightWith(other: HasWeights) {
     this.selfWeight = other.selfWeight
     this.totalWeight = other.totalWeight
+    this.selfSamples = other.selfSamples
+    this.totalSamples = other.totalSamples
   }
 }

@@ -355,7 +371,7 @@ export class Profile {
       for (let n: CallTreeNode | null = node; n != null && n.frame !== Frame.root; n = n.parent) {
         stack.push(n.frame)
       }
-      builder.appendSampleWithWeight(stack, node.getTotalWeight())
+      builder.appendSampleWithWeight(stack, node.getTotalWeight(), node.getTotalSamples())
     }

     const ret = builder.build()
@@ -373,7 +389,7 @@ export class Profile {

       function visit(node: CallTreeNode) {
         stack.push(node.frame)
-        builder.appendSampleWithWeight(stack, node.getSelfWeight())
+        builder.appendSampleWithWeight(stack, node.getSelfWeight(), node.getSelfSamples())
         for (let child of node.children) {
           visit(child)
         }
@@ -401,25 +417,9 @@ export class Profile {
     return ret
   }

-  // Demangle symbols for readability
-  async demangle() {
-    let demangle: ((name: string) => string) | null = null
-
-    for (let frame of this.frames) {
-      // This function converts a mangled C++ and Rust name into a human-readable symbol.
-      if (
-        frame.name.startsWith('__Z') ||
-        frame.name.startsWith('_R') ||
-        frame.name.startsWith('_Z')
-      ) {
-        if (!demangle) {
-          const demangleModule = await import('./demangle')
-          demangle = await demangleModule.loadDemangling()
-        }
-        frame.name = demangle(frame.name)
-      }
-    }
-  }
+  // Zakura already supplies bounded demangled names and retains raw symbols separately.
+  // Do not load the redundant WASM demangler or alter canonical frame names here.
+  async demangle() {}

   remapSymbols(callback: SymbolRemapper) {
     for (let frame of this.frames) {
@@ -445,7 +445,12 @@ export class Profile {
 }

 export class StackListProfileBuilder extends Profile {
-  _appendSample(stack: Frame[], weight: number, useAppendOrder: boolean) {
+  _appendSample(
+    stack: Frame[],
+    weight: number,
+    useAppendOrder: boolean,
+    sampleCount: number | null,
+  ) {
     if (isNaN(weight)) throw new Error('invalid weight')
     let node = useAppendOrder ? this.appendOrderCalltreeRoot : this.groupedCalltreeRoot

@@ -463,6 +468,7 @@ export class StackListProfileBuilder extends Profile {
         parent.children.push(node)
       }
       node.addToTotalWeight(weight)
+      node.addToTotalSamples(sampleCount)

       // It's possible for the same frame to occur multiple
       // times in the same call stack due to either direct
@@ -473,6 +479,7 @@ export class StackListProfileBuilder extends Profile {
       framesInStack.add(node.frame)
     }
     node.addToSelfWeight(weight)
+    node.addToSelfSamples(sampleCount)

     if (useAppendOrder) {
       for (let child of node.children) {
@@ -482,9 +489,11 @@ export class StackListProfileBuilder extends Profile {

     if (useAppendOrder) {
       node.frame.addToSelfWeight(weight)
+      node.frame.addToSelfSamples(sampleCount)

       for (let frame of framesInStack) {
         frame.addToTotalWeight(weight)
+        frame.addToTotalSamples(sampleCount)
       }

       if (node === lastOf(this.samples)) {
@@ -496,7 +505,7 @@ export class StackListProfileBuilder extends Profile {
     }
   }

-  appendSampleWithWeight(stack: FrameInfo[], weight: number) {
+  appendSampleWithWeight(stack: FrameInfo[], weight: number, sampleCount: number | null = 1) {
     if (weight === 0) {
       // Samples with zero weight have no effect, so let's ignore them
       return
@@ -506,8 +515,8 @@ export class StackListProfileBuilder extends Profile {
     }

     const frames = stack.map(fr => Frame.getOrInsert(this.frames, fr))
-    this._appendSample(frames, weight, true)
-    this._appendSample(frames, weight, false)
+    this._appendSample(frames, weight, true, sampleCount)
+    this._appendSample(frames, weight, false, sampleCount)
   }

   private pendingSample: {
diff --git a/src/lib/value-formatters.test.ts b/src/lib/value-formatters.test.ts
index b147e58..69f492a 100644
--- a/src/lib/value-formatters.test.ts
+++ b/src/lib/value-formatters.test.ts
@@ -3,12 +3,12 @@ import {TimeFormatter, ByteFormatter} from './value-formatters'
 describe('TimeFormatter', () => {
   test('input units milliseconds', () => {
     const f = new TimeFormatter('milliseconds')
-    expect(f.format(0.00004)).toEqual('40.00ns')
-    expect(f.format(0.04)).toEqual('40.00µs')
-    expect(f.format(3)).toEqual('3.00ms')
-    expect(f.format(2070)).toEqual('2.07s')
-    expect(f.format(150000)).toEqual('2:30')
-    expect(f.format(1203123)).toEqual('20:03')
+    expect(f.format(0.00004)).toEqual('0.00 ms')
+    expect(f.format(0.04)).toEqual('0.04 ms')
+    expect(f.format(3)).toEqual('3.00 ms')
+    expect(f.format(2070)).toEqual('2070.00 ms')
+    expect(f.format(150000)).toEqual('150000.00 ms')
+    expect(f.format(1203123)).toEqual('1203123.00 ms')
   })

   test('input units seconds', () => {
@@ -28,3 +28,10 @@ test('ByteFormatter', () => {
   expect(f.format(3.5 * 1024 * 1024)).toEqual('3.50 MB')
   expect(f.format(4.32 * 1024 * 1024 * 1024)).toEqual('4.32 GB')
 })
+
+test('millisecond profiles keep one explicit unit even at zero', () => {
+  const f = new TimeFormatter('milliseconds')
+  expect(f.format(0)).toBe('0.00 ms')
+  expect(f.format(0.01)).toBe('0.01 ms')
+  expect(f.format(1200)).toBe('1200.00 ms')
+})
diff --git a/src/lib/value-formatters.ts b/src/lib/value-formatters.ts
index 5635eed..ac28758 100644
--- a/src/lib/value-formatters.ts
+++ b/src/lib/value-formatters.ts
@@ -24,6 +24,7 @@ export class TimeFormatter implements ValueFormatter {
   }

   formatUnsigned(v: number) {
+    if (this.unit === 'milliseconds') return `${v.toFixed(2)} ms`
     const s = v * this.multiplier

     if (s / 60 >= 1) {
diff --git a/src/views/application.tsx b/src/views/application.tsx
index be24242..c2cb5c6 100644
--- a/src/views/application.tsx
+++ b/src/views/application.tsx
@@ -28,7 +28,7 @@ const importModule = import('../import')
 // We put them all in one place so we can directly control the relative priority
 // of these.
 importModule.then(() => {})
-import('../lib/demangle').then(() => {})
+// The host supplies canonical demangled names; no WASM demangler preload.
 import('source-map').then(() => {})

 async function importProfilesFromText(
diff --git a/src/views/flamechart-pan-zoom-view.tsx b/src/views/flamechart-pan-zoom-view.tsx
index 78e7cf1..9d8e5c1 100644
--- a/src/views/flamechart-pan-zoom-view.tsx
+++ b/src/views/flamechart-pan-zoom-view.tsx
@@ -1,3 +1,4 @@
+import {compactFrame} from '../lib/compact-frame'
 import {Rect, AffineTransform, Vec2, clamp} from '../lib/math'
 import {CallTreeNode} from '../lib/profile'
 import {Flamechart, FlamechartFrame} from '../lib/flamechart'
@@ -235,7 +236,7 @@ export class FlamechartPanZoomView extends Component<FlamechartPanZoomViewProps,

           const trimmedText = trimTextMid(
             ctx,
-            frame.node.frame.name,
+            match ? frame.node.frame.name : compactFrame(frame.node.frame.name),
             physicalLabelBounds.width() - 2 * LABEL_PADDING_PX,
           )

@@ -579,7 +580,7 @@ export class FlamechartPanZoomView extends Component<FlamechartPanZoomViewProps,
     }
   }

-  private onDblClick = (ev: MouseEvent) => {
+  private zoomToHoveredFrame = () => {
     if (this.hoveredLabel) {
       const hoveredBounds = this.hoveredLabel.configSpaceBounds
       const viewportRect = new Rect(
@@ -603,6 +604,7 @@ export class FlamechartPanZoomView extends Component<FlamechartPanZoomViewProps,

     if (this.hoveredLabel) {
       this.props.onNodeSelect(this.hoveredLabel.node)
+      this.zoomToHoveredFrame()
       this.renderCanvas()
     } else {
       this.props.onNodeSelect(null)
@@ -862,7 +864,6 @@ export class FlamechartPanZoomView extends Component<FlamechartPanZoomViewProps,
         onMouseMove={this.onMouseMove}
         onMouseLeave={this.onMouseLeave}
         onClick={this.onClick}
-        onDblClick={this.onDblClick}
         onWheel={this.onWheel}
         ref={this.containerRef}
       >
diff --git a/src/views/flamechart-view.tsx b/src/views/flamechart-view.tsx
index 0ae7854..2b63f58 100644
--- a/src/views/flamechart-view.tsx
+++ b/src/views/flamechart-view.tsx
@@ -87,9 +87,15 @@ export class FlamechartView extends StatelessComponent<FlamechartViewProps> {
     return (
       <Hovertip containerSize={new Vec2(width, height)} offset={offset}>
         <span className={css(style.hoverCount)}>
-          {this.formatValue(hover.node.getTotalWeight())}
-        </span>{' '}
-        {frame.name}
+          Total (including calls): {this.formatValue(hover.node.getTotalWeight())}
+          {hover.node.getTotalSamples() !== null
+            ? ` · ${hover.node.getTotalSamples()} samples`
+            : ''}
+          <br />
+          Self (this function): {this.formatValue(hover.node.getSelfWeight())}
+          {hover.node.getSelfSamples() !== null ? ` · ${hover.node.getSelfSamples()} samples` : ''}
+        </span>
+        <div>{frame.name}</div>
         {frame.file ? (
           <div>
             {frame.file}:{frame.line}
diff --git a/src/views/flamechart-wrapper.tsx b/src/views/flamechart-wrapper.tsx
index 409358e..beebbab 100644
--- a/src/views/flamechart-wrapper.tsx
+++ b/src/views/flamechart-wrapper.tsx
@@ -46,9 +46,15 @@ export class FlamechartWrapper extends StatelessComponent<FlamechartViewProps> {
     return (
       <Hovertip containerSize={new Vec2(width, height)} offset={offset}>
         <span className={css(style.hoverCount)}>
-          {this.formatValue(hover.node.getTotalWeight())}
-        </span>{' '}
-        {frame.name}
+          Total (including calls): {this.formatValue(hover.node.getTotalWeight())}
+          {hover.node.getTotalSamples() !== null
+            ? ` · ${hover.node.getTotalSamples()} samples`
+            : ''}
+          <br />
+          Self (this function): {this.formatValue(hover.node.getSelfWeight())}
+          {hover.node.getSelfSamples() !== null ? ` · ${hover.node.getSelfSamples()} samples` : ''}
+        </span>
+        <div>{frame.name}</div>
         {frame.file ? (
           <div>
             {frame.file}:{frame.line}
diff --git a/src/views/profile-table-view.tsx b/src/views/profile-table-view.tsx
index 9a429dc..67c6485 100644
--- a/src/views/profile-table-view.tsx
+++ b/src/views/profile-table-view.tsx
@@ -1,3 +1,4 @@
+import {compactFrame} from '../lib/compact-frame'
 import {h, JSX, ComponentChild} from 'preact'
 import {StyleSheet, css} from 'aphrodite'
 import {Profile, Frame} from '../lib/profile'
@@ -131,7 +132,7 @@ const ProfileTableRowView = ({
         {profile.formatValue(selfWeight)} ({formatPercent(selfPerc)})
         <HBarDisplay perc={selfPerc} />
       </td>
-      <td title={frame.file} className={css(style.textCell)}>
+      <td title={frame.name} className={css(style.textCell)}>
         <ColorChit color={getCSSColorForFrame(frame)} />
         {matchedRanges
           ? highlightRanges(
@@ -139,7 +140,7 @@ const ProfileTableRowView = ({
               matchedRanges,
               css(style.matched, selected && style.matchedSelected),
             )
-          : frame.name}
+          : compactFrame(frame.name)}
       </td>
     </tr>
   )
diff --git a/src/views/toolbar.tsx b/src/views/toolbar.tsx
index a47372c..562076f 100644
--- a/src/views/toolbar.tsx
+++ b/src/views/toolbar.tsx
@@ -39,7 +39,7 @@ function ToolbarLeftContent(props: ToolbarProps) {
         )}
         onClick={setChronoFlameChart}
       >
-        <span className={css(style.emoji)}>🕰</span>Time Order
+        <span className={css(style.emoji)}>🕰</span>Sample Order
       </div>
       <div
         className={css(
