<!-- changelog: none -->

This PR only adds CI tooling: a manually dispatched mempool load-test workflow,
its supporting scripts and tests, and a lint job that runs the repository's
existing Python test suites. It has no operator- or crate-consumer-visible
effect on zakurad or the published crates.
