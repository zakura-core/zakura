## Security

- Reject nested collection counts that cannot fit in complete Block and headers
  messages before reserving memory. Use the configured network's header sizes
  so Regtest's smaller encoding does not loosen Mainnet or Testnet checks
  ([#974](https://github.com/zakura-core/zakura/pull/974)).
