## Fixed

- Reserve auxiliary storage for the next VCT block commit and its authentication
  successor across fork changes. Replace non-authoritative input in full repair
  buckets, including after restart. Keep speculative authentication repair within
  its capacity budget. Report a continuous capacity wait through daemon shutdown
  ([#950](https://github.com/zakura-core/zakura/pull/950)).
- Stop the daemon when the block writer fails, including when read-state clones
  still hold its task handle
  ([#950](https://github.com/zakura-core/zakura/pull/950)).
