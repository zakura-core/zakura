## Fixed

- Safely reuse released format-28 databases for the format-29 upgrade. Lock the source before checkpointing and preserve its format marker so startup cannot move a running node's database or skip required migrations. ([#1079](https://github.com/zakura-core/zakura/pull/1079)).
