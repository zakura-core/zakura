## Changed

- The node writes commit-state and legacy peer request traces as CSV to reduce repeated field names. Other trace tables retain JSONL. Both formats include absolute UTC timestamps ([#965](https://github.com/zakura-core/zakura/pull/965)).
