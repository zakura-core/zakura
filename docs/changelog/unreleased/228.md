<!-- changelog: none -->

This PR only refactors how Zakura JSONL trace events are constructed and
emitted — lazily built typed events, localized in per-module `trace.rs`
modules. The emitted trace schema is unchanged, so there is no operator- or
crate-consumer-visible effect.
