## Fixed

- Zakura now fails fast and restarts when an accepted Full block commit remains
  stalled, rather than waiting for the legacy fallback drain deadline or
  overlapping a non-cancellable state write
  ([#1091](https://github.com/zakura-core/zakura/pull/1091)).
