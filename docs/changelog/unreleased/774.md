## Fixed

- Warn when an indexer RPC TLS server certificate is expired or not yet valid, allowing
  operators to diagnose listeners that clients reject
  ([#774](https://github.com/zakura-core/zakura/pull/774)).
