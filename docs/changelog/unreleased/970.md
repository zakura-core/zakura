## Fixed

- Fixed native sync stalling after its only VCT auxiliary supplier temporarily
  replies busy. Retry that supplier with bounded backoff while keeping other
  suppliers available, and clear temporary retry history on disconnect or session
  replacement so a reconnected supplier can be used immediately. Rotate suppliers
  after successful repairs and refusals so each waiting peer gets a fair turn.
  Keep serving unchanged header paths while the node syncs, with coherent page
  snapshots and finalized metadata matched to the requested branch
  ([#970](https://github.com/zakura-core/zakura/pull/970)).
