## Fixed

- Fixed native sync stalling after its only VCT auxiliary supplier temporarily
  replies busy. Retry that supplier with bounded backoff while keeping other
  suppliers available, and clear temporary retry history on disconnect so a
  reconnected supplier can be used immediately
  ([#970](https://github.com/zakura-core/zakura/pull/970)).
