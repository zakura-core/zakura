## Fixed

- `submitblock` checks proof of work in a bounded blocking task before reserving a
  contextual commit slot. Concurrent requests share a 16-check limit and reject
  duplicate pending proof checks. RPC cancellation retains the limit until the
  proof check finishes. PoW waivers and block proposals still enforce the active
  network's solution encoding
  ([#946](https://github.com/zakura-core/zakura/pull/946)).
