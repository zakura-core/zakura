## Added

- Add parent-dependent NU7 reissuance and negative-deficit rejection behind the disabled-by-default `nu7` feature. Block proposals reject a parent that has not committed instead of waiting for it. Production activation remains pending policy and network parameters. ([#1053](https://github.com/zakura-core/zakura/pull/1053)).
