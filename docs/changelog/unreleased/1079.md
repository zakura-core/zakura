## Fixed

- Reuse eligible state caches across multiple major versions while preserving interrupted upgrades' recorded versions. Checkpoint older caches under exclusive locks, retaining the source and sibling network caches, and report unusable or locked caches instead of silently starting fresh. ([#1079](https://github.com/zakura-core/zakura/pull/1079)).
- Keep ephemeral database version files in the database's own temporary directory without allocating extra directories during discovery and version checks. ([#1079](https://github.com/zakura-core/zakura/pull/1079)).
