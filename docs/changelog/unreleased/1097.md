<!-- changelog: none -->

Deploy and operator tooling for the NU7 fork testnet. The deployer can now
render a configured testnet's `[network.network]` table, an explicit peer list,
and a `[mining]` section. `zakurad` behaviour and its public APIs are unchanged.
