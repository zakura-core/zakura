## Fixed

- Fixed concurrent mining template requests returning transient errors after a tip
  change and clearing newer template rejection records. Superseded requests now
  retry with fresh state, avoiding the internal miner's error backoff
  ([#1083](https://github.com/zakura-core/zakura/pull/1083)).
