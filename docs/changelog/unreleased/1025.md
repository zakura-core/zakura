## Fixed

- Zakura block sync now schedules a body without a known size hint with a moving average of recently accepted body sizes instead of the 2 MB worst case. Small blocks from peers that send no size hints can therefore use the available download capacity. Memory admission still charges these bodies at the worst case
  ([#1025](https://github.com/zakura-core/zakura/pull/1025)).
