<!-- changelog: none -->

This PR only moves repository-level docs and tool configuration (community
health files into `docs/`, `release.toml` into `[workspace.metadata.release]`,
`supply-chain/` into `qa/`, and the vestigial `book/` and `.trunk/` trees
removed). The `Cargo.toml` change relocates existing cargo-release settings
verbatim; there is no operator- or crate-consumer-visible effect.
