## Changed

- Serve mining templates from a shared background updater so concurrent
  `getblocktemplate` requests reuse coinbase proofs. Check cached templates against
  the committed chain tip and preserve template withdrawal and validated recovery
  ([#1058](https://github.com/zakura-core/zakura/pull/1058)).
